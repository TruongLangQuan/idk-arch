#################
### AUTOSTART ###
#################

xwayland {
  force_zero_scaling = true
}

exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data
exec-once = systemctl --user start hyprpolkitagent
exec-once = hypridle
exec-once = sleep 1 && waybar

# set a random wallpaper at start and start hyprpaper
# exec-once = awww-daemon & sleep 1; $HOME/.config/hypr/scripts/wallpapers/set-random.sh
# exec-once = kitty -o window_margin_width=0 -o background_opacity=0.25 --class wallpaper_cmatrix -e cmatrix -b -C green -u 6
